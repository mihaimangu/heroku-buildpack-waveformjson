#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Install libsndfile1-dev"
BUILD_DIR=$1
VENDOR_DIR="vendor" 



cd $BUILD_DIR
mkdir -p $VENDOR_DIR
cd $VENDOR_DIR
mkdir -p ruby-audio
cd ruby-audio

apt-get install libsndfile1-dev

